{
  "name": "StepsProno",
  "version": "2.1.6",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "14.x"
  }
}
